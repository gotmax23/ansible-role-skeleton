# SPDX-FileCopyrightText: 2021 Maxwell G (@gotmax23)
# SPDX-License-Identifier: CC0-1.0
---
# Options:
# - `present` ensures that {{ role_name | regex_replace('_' ' ') | title }} is installed.
# - `absent` ensures that {{ role_name | regex_replace('_' ' ') | title }} is not installed.
{{ role_name }}_state: present
